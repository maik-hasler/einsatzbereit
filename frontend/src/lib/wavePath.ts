// Gentle two-hump wave used to cap the full-bleed brand bands on the
// marketing homepage (the founder section) and the footer - the same shape
// everywhere so the "wavy edge" reads as one deliberate motif rather than a
// one-off. Flip it with a `rotate-180` className for a bottom cap.
export const WAVE_PATH =
	"M0,20 C 240,60 480,0 720,20 C 960,40 1200,0 1440,20 L1440,60 L0,60 Z";
