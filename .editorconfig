# https://editorconfig.org
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = tab
trim_trailing_whitespace = true
insert_final_newline = true

# Markdown: code blocks use space indentation; size varies (list continuation, trees)
[*.md]
indent_style = space
trim_trailing_whitespace = false
