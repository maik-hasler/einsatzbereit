services:
  keycloak:
    entrypoint: ["/opt/keycloak/bin/kc.sh"]
    command: ["start-dev", "--import-realm"]
    environment:
      KC_DB: dev-mem
    healthcheck:
      start_period: 30s
      retries: 12
